# 目标 (Objective)

在项目根目录创建 `GEMINI.md`，为后续的 Gemini Agent 交互提供持久化的项目级上下文指导。

## 修改步骤 (Changes)

- 在项目根目录 (`/home/zcq/Github/clouddrive-auto-save/`) 新建 `GEMINI.md` 文件。
- 将确认过的项目上下文（架构、构建说明、纯中文规范等）写入其中。

## 验证步骤 (Verification)

- 确认根目录下存在 `GEMINI.md` 且内容正确。
