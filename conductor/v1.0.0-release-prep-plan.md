# v1.0.0 版本发布准备计划 (v1.0.0 Release Prep Plan)

**Goal:** 为即将发布的 v1.0.0 版本做准备，更新文档中的 Docker 镜像标签，将 `latest` 替换为明确的版本号 `v1.0.0`。

---

### Task 1: 更新 README.md 中的镜像标签

**Files:**
- Modify: `README.md`

**Changes:**
1. 将 `docker run` 命令中的 `zcq98/clouddrive-auto-save:latest` 替换为 `zcq98/clouddrive-auto-save:v1.0.0`。
2. 将 `docker-compose.yml` 示例中的 `image: zcq98/clouddrive-auto-save:latest` 替换为 `image: zcq98/clouddrive-auto-save:v1.0.0`。

### Task 2: 自动提交与推送

- 提交代码更改。
- 自动执行推送。
